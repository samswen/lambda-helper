{
  "name": "@samwen/lambda-helper",
  "version": "1.0.1",
  "description": "An effort to enable single lambda handles functionally the same requests from different sources: console test, sns, sqs, api gateway and api invoke. It also addresses AWS lambda EMFILE issue.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "AWS",
    "lambda",
    "console test",
    "sns", 
    "sqs",
    "api gateway",
    "api invoke",
    "EMFILE",
    "file descriptor",
    "limit",
    "leak",
    "Timeout",
    "ioredis",
    "mongodb",
    "AWS Lambda function errors",
    "MongoServerSelectionError",
    "Failed to refresh slots cache",
    "getaddrinfo EMFILE"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/samswen/lambda-helper"
  },
  "homepage": "https://github.com/samswen/lambda-helper#readme",
  "author": "Sam Wen",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "eslint": "^8.2.0",
    "mocha": "^9.1.3"
  }
}
